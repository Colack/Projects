import fs from 'fs';

// Create a new File that has a chosen log, plus some extra features.
export function createLog(code, message) {
 	
 	// Pull a random Number
  var x = getRandomInt();
  
  // Create
  
	// Create content of the File


	// Create a random number
  function getRandomInt() {
  	return Math.floor(Math.random() * 3);
	}
}
